.DS_Store
.ipynb_checkpoints

# Windows Explorer thumbnail caches -- 215 of these were committed alongside the
# OSHPD chargemaster downloads (untracked from the index in Aug 2026; ignored
# here so they never come back).
Thumbs.db
ehthumbs.db
